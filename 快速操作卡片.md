# 🚀 快速操作卡片

## 📝 第一次使用（只需做一次）

### 1. 在GitHub网站创建仓库
```
访问：https://github.com
点击：+ → New repository
填写：仓库名（如 music-app）
选择：Public（公开）
不勾选任何复选框
点击：Create repository
复制：仓库地址（https://github.com/用户名/仓库名.git）
```

### 2. 配置Git（PowerShell执行）
```powershell
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的邮箱"
```

### 3. 推送代码
```powershell
cd "E:\项目\Music app"
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin master
```

### 4. 下载APK
```
GitHub仓库页面 → Actions → 点击工作流 → 等待完成 → 下载 Artifacts
```

---

## 🔄 以后每次修改代码

```powershell
cd "E:\项目\Music app"
git add -A
git commit -m "修改说明"
git push
```

推送后GitHub自动构建，5-10分钟后可下载新APK。

---

## 🆘 遇到问题？

**推送失败** → 检查网络，确认登录GitHub
**构建失败** → Actions页面查看错误日志
**APK安装失败** → 允许手机安装未知来源应用

详细步骤请看：**新手GitHub操作指南.md**