# 音频服务器部署指南

本指南帮助你将音频文件部署到服务器，实现远程播放。

## 📋 方案概述

将音频文件上传到服务器后，网页版和Android APP都可以通过网络播放服务器上的音乐，无需在本地存储大量文件。

## 🎯 部署步骤

### 方案A：使用云存储服务（推荐）

#### 1. 阿里云OSS / 腾讯云COS / 七牛云

**优点**：
- CDN加速，播放流畅
- 按需付费，成本低
- 管理方便

**步骤**：

1. **注册并创建存储桶**
   - 访问云服务商官网注册账号
   - 创建一个存储桶（Bucket）
   - 设置为**公开读**或配置访问权限

2. **上传音频文件**
   ```
   保持原有目录结构：
   music/
   ├── 粤语歌曲/
   │   ├── 今天为你点亮/
   │   │   ├── 今天为你点亮.wav
   │   │   ├── 今天为你点亮.lrc
   │   │   └── 今天为你点亮.png
   │   └── ...
   └── 治愈歌曲/
       └── ...
   ```

3. **获取访问地址**
   - 记下存储桶的公网访问域名
   - 例如：`https://your-bucket.oss-cn-hangzhou.aliyuncs.com`

4. **配置应用**
   编辑 `web/scripts/config.js`：
   ```javascript
   export const serverConfig = {
       useRemoteServer: true,
       serverBaseUrl: 'https://your-bucket.oss-cn-hangzhou.aliyuncs.com/music',
       auth: {
           enabled: false
       }
   };
   ```

5. **更新歌曲列表**
   编辑 `web/scripts/data/tracks.js`，修改音频路径：
   ```javascript
   src: "assets/audio/粤语歌曲/今天为你点亮/今天为你点亮.wav"
   // 路径会自动添加服务器前缀，变成：
   // https://your-bucket.oss-cn-hangzhou.aliyuncs.com/music/assets/audio/...
   ```

---

### 方案B：自建Web服务器

#### 1. 使用简单HTTP服务器

如果你有自己的服务器或VPS：

**在Linux服务器上：**

```bash
# 1. 创建音频目录
mkdir -p /var/www/music
cd /var/www/music

# 2. 上传音频文件到这个目录
# 使用 FTP、SCP 或 其他方式上传

# 3. 启动HTTP服务器
# 方式A：使用Python（推荐，简单）
python3 -m http.server 8080

# 方式B：使用Nginx
sudo apt install nginx
# 配置Nginx指向 /var/www/music 目录
```

**Nginx配置示例** (`/etc/nginx/sites-available/music`):
```nginx
server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名或IP
    
    root /var/www/music;
    
    # 启用CORS（允许跨域访问）
    add_header 'Access-Control-Allow-Origin' '*' always;
    add_header 'Access-Control-Allow-Methods' 'GET, OPTIONS' always;
    add_header 'Access-Control-Allow-Headers' 'Range' always;
    
    location / {
        autoindex on;  # 可选：启用目录列表
        try_files $uri $uri/ =404;
    }
    
    # 音频文件缓存设置
    location ~* \.(mp3|wav|flac|m4a|ogg)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

启用配置：
```bash
sudo ln -s /etc/nginx/sites-available/music /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### 2. 配置应用

编辑 `web/scripts/config.js`：
```javascript
export const serverConfig = {
    useRemoteServer: true,
    serverBaseUrl: 'http://your-domain.com',  // 或 'http://192.168.1.100:8080'
    auth: {
        enabled: false
    }
};
```

---

### 方案C：使用GitHub Pages（免费，但有限制）

**限制**：
- 文件大小不能超过100MB
- 总仓库大小建议不超过1GB
- 适合小型项目

**步骤**：

1. 创建单独的音频仓库
   ```bash
   # 创建新仓库 music-files（设为公开）
   ```

2. 上传音频文件到该仓库

3. 在仓库设置中启用GitHub Pages

4. 配置应用使用GitHub Pages地址：
   ```javascript
   serverBaseUrl: 'https://your-username.github.io/music-files'
   ```

---

## ⚙️ 应用配置修改

### 1. 修改配置文件

编辑 `web/scripts/config.js`，根据你选择的方案填写：

```javascript
export const serverConfig = {
    useRemoteServer: true,
    serverBaseUrl: 'https://your-server.com/music',
    auth: {
        enabled: false  // 如果服务器需要认证，改为true并配置
    }
};
```

### 2. 更新代码使用配置

需要修改 `web/scripts/core/audio-engine.js` 或相关模块，在加载音频时使用 `getAudioUrl()` 函数。

示例：
```javascript
import { getAudioUrl } from '../config.js';

// 加载音频时
const audioSrc = getAudioUrl(track.src);
audio.src = audioSrc;
```

### 3. 测试

1. 先在本地浏览器打开 `web/index.html` 测试
2. 打开浏览器控制台（F12）查看是否有错误
3. 检查音频是否能正常加载和播放

---

## 🔧 跨域问题解决

### 问题：浏览器控制台显示CORS错误

**解决方法A：服务器端配置CORS**

在服务器响应头中添加：
```
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET, OPTIONS
Access-Control-Allow-Headers: Range
```

**解决方法B：使用CORS代理**

配置文件中启用代理：
```javascript
cors: {
    useProxy: true,
    proxyUrl: 'https://cors-anywhere.herokuapp.com/'
}
```

---

## 📱 Android APP配置

Android APP会自动使用相同的配置，因为它是基于WebView加载网页版的。

确保在Android项目中：

1. `AndroidManifest.xml` 已添加网络权限：
   ```xml
   <uses-permission android:name="android.permission.INTERNET" />
   <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
   ```

2. 如果使用HTTP（非HTTPS），需要允许明文流量：
   ```xml
   <application
       android:usesCleartextTraffic="true"
       ...>
   ```

---

## 🧪 测试清单

部署完成后，检查以下项目：

- [ ] 服务器上文件结构正确
- [ ] 音频文件可以通过浏览器直接访问（粘贴完整URL到浏览器地址栏）
- [ ] 配置文件已正确填写服务器地址
- [ ] 网页版能正常播放
- [ ] Android APP能正常播放
- [ ] 歌词能正确加载和显示
- [ ] 封面图片能正常显示
- [ ] 播放进度可以正常拖动

---

## 💰 成本估算

### 云存储（以阿里云OSS为例）

**存储费用**：
- 标准存储：0.12元/GB/月
- 10GB音乐文件：约1.2元/月

**流量费用**：
- 外网流出流量：0.5元/GB
- 每月听100首歌（约500MB）：约0.25元

**总计**：约1.5-2元/月

### 自建服务器

- 如果已有服务器或VPS：几乎无额外成本
- 购买云服务器：约10-50元/月（最低配置）

---

## 🆘 常见问题

### Q1: 音频无法播放，控制台显示404
**解决**：
- 检查服务器地址是否正确
- 检查文件路径是否匹配
- 确认文件已成功上传到服务器

### Q2: 播放卡顿
**解决**：
- 使用CDN加速
- 压缩音频文件（降低比特率）
- 选择距离用户更近的服务器节点

### Q3: 手机播放正常，电脑播放不了
**解决**：
- 检查浏览器是否支持音频格式
- 尝试转换为更通用的格式（如MP3）

### Q4: HTTPS网页无法播放HTTP音频
**解决**：
- 使用HTTPS服务器
- 或使用云存储（通常默认支持HTTPS）

---

## 📚 推荐阅读

- [阿里云OSS文档](https://help.aliyun.com/product/31815.html)
- [腾讯云COS文档](https://cloud.tencent.com/document/product/436)
- [Nginx配置教程](https://nginx.org/en/docs/)
- [HTML5 Audio API](https://developer.mozilla.org/zh-CN/docs/Web/HTML/Element/audio)

---

## 🎯 下一步

部署完成后：

1. 提交代码到GitHub：
   ```bash
   git add .
   git commit -m "配置远程音频服务器支持"
   git push
   ```

2. GitHub Actions会自动构建新的APK

3. 下载并测试新版本APP

需要帮助配置？随时告诉我你选择哪种方案！